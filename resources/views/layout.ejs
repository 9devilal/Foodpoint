<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Foodpoint</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300&display=swap" rel="stylesheet"> 
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    <link rel="stylesheet" href="/css/app.css">
    

</head>
<body>
    <nav class = "container mx-auto flex items-center justify-between py-4 text-sm md:text-lg">
        <div id = "logo" class ="inline-block px-4 py-2 rounded-full">
            <a href="/" ><b>Foodpoint</b></a>
        </div>
        <div>
             <ul class="flex items-center">
                 <li class="ml-6"><a href ="/">Menu</a></li>
                 <%if(!user){%>


                 <li class="ml-6"><a href ="/login">LogIn</a></li>
                 <li class="ml-6"><a href ="/register">Register</a></li>
                    
                 <%}else if(user.role === 'worker'){%>
                      
                    <li class="ml-6"><a href ="/worker/orders">Active Orders</a></li>
                    <li class="ml-6">
                    <form action="/logout" id="logout" method="POST">
                 <a onclick="document.getElementById('logout').submit()" href="#">Logout</a>
                </form>
                </li>


                 <%} else{%>

                    <li class="ml-6"><a href ="/customer/orders">My Orders!</a></li>
                    <li class="ml-6">
                    <form action="/logout" id="logout" method="POST">
                 <a onclick="document.getElementById('logout').submit()" href="#">Logout</a>
                </form>
                </li>


                    <%}%>
                 <li class="ml-6"><a href ="/cart" class="inline-block px-4 py-2 rounded-full flex justify-center">
                    <span id = "cartCounter" class="px-2 font-bold"><%=session.cart ? session.cart.totalQty : ''%></span>
                    <img src="/img/cart.png"></a></li>
             </ul>
        </div>
    </nav>
    <%-body%>
    <script src="/socket.io/socket.io.js"></script>
    <script src = "/js/app.js"></script>
</body>
</html>